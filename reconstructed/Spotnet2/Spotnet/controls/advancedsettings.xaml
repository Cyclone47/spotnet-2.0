<metro:MetroWindow x:Class="Spotnet.Controls.AdvancedSettings" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls" xmlns:p="clr-namespace:Spotnet.Properties" Title="{x:Static p:Words.AdvancedSettings}" ShowInTaskbar="False" ResizeMode="CanResizeWithGrip" SizeToContent="Manual" WindowStartupLocation="CenterOwner" Topmost="False" GlowBrush="{DynamicResource HighlightBrush}" Height="500" Width="680" WindowTransitionsEnabled="True" ShowIconOnTitleBar="False" ShowTitleBar="True" ShowActivated="True" xmlns:controls="clr-namespace:Spotnet.Controls" FontSize="{Binding ElementName=VisibilityVmProxy, Path=DataContext.FontSize, Mode=OneWay}">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <Storyboard x:Key="FieldAnimationStoryboard">
        <ColorAnimation Storyboard.TargetProperty="BorderBrush.Color" From="White" To="OrangeRed" Duration="0:0:0.5" AutoReverse="True" RepeatBehavior="Forever" />
      </Storyboard>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Control.Background>
    <LinearGradientBrush EndPoint="0,1" StartPoint="0.5,0">
      <GradientStop Color="#FFEBF3FF" Offset="0.3" />
      <GradientStop Color="#FFD0D7FF" Offset="0.6" />
    </LinearGradientBrush>
  </Control.Background>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ListBox Name="SettingsHeadersListBox" Grid.Row="0" Grid.Column="0" VerticalAlignment="Stretch" BorderThickness="1" Margin="10,10,10,0" Background="#CCFFFFFF" FontSize="{DynamicResource WindowTitleFontSize}" Width="180" ItemsSource="{Binding SettingHeaders}" SelectedIndex="{Binding HeaderItemIndex}" />
    <Grid Margin="5,10,10,0" Column="1" Row="0" VerticalAlignment="Stretch">
      <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,1">
        <Grid Name="ContentGrid" />
      </ScrollViewer>
      <Separator VerticalAlignment="Bottom" />
    </Grid>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="5" Grid.Column="1" Grid.Row="1">
      <Button Name="OkButton" Margin="5" Content="{x:Static p:Words.Ok}" Height="25" Width="90" HorizontalAlignment="Right" IsDefault="False" IsEnabled="True" Click="OkButton_Click" />
      <Button Name="CancelButton" Margin="5" Content="{x:Static p:Words.Cancel}" Height="25" Width="90" HorizontalAlignment="Right" IsDefault="False" IsEnabled="True" Click="CancelButton_Click" />
    </StackPanel>
  </Grid>
</metro:MetroWindow>